{
  "name": "alpha-seeker",
  "version": "1.0.0",
  "description": "Mobile-first Solana trading intelligence platform for the retail trader",
  "private": true,
  "scripts": {
    "mobile:start": "cd apps/mobile && npm start",
    "mobile:build": "cd apps/mobile && npm run build",
    "mobile:android": "cd apps/mobile && npm run android",
    "mobile:ios": "cd apps/mobile && npm run ios",
    "mobile:build:check": "cd apps/mobile && npm run build:check",
    "mobile:validate": "cd apps/mobile && npm run validate",
    "api:start": "cd apps/api && npm start",
    "api:build": "cd apps/api && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}